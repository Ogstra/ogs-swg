services:
  ogs-swg:
    build: .
    image: ${OGS_IMAGE}
    container_name: ogs-swg
    restart: unless-stopped
    ports:
      - "${OGS_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
      # Mount SSH Key if using remote management
      - ./config/ssh_key:/app/secrets/ssh_key:ro
      - /etc/sing-box:/etc/sing-box
      - /etc/wireguard:/etc/wireguard
    environment:
      - OGS_BIND=${OGS_BIND:-:8080}
      - OGS_DB_PATH=${OGS_DB_PATH:-/app/data/stats.db}
      # SSH Configuration for Remote Management
      - OGS_SSH_HOST=${OGS_SSH_HOST}
      - OGS_SSH_PORT=${OGS_SSH_PORT}
      - OGS_SSH_USER=${OGS_SSH_USER}
      - OGS_SSH_KEY_PATH=${OGS_SSH_KEY_PATH}
      - OGS_SSH_KEY_PASS=${OGS_SSH_KEY_PASS}
    # capabilities might be needed if running local wireguard inside container (not recommended mode here)
    # cap_add:
    #   - NET_ADMIN
