# OGS-SWG — blue slot (docker_local mode)
#
# Panel in Docker on the SAME host as sing-box/wg (systemd).
# No SSH connection needed — talks to host systemd via bind-mounted runtime sockets.
# Use this instead of docker-compose.blue.yml when deploying locally.
#
# See DEPLOY_GITHUB_ACTIONS.md — Docker Local Mode section.

services:
  ogs-swg-blue:
    image: ${OGS_IMAGE}
    container_name: ogs-swg-blue
    restart: unless-stopped
    network_mode: host

    volumes:
      - ../../data:/app/data
      - ${OGS_ACCESS_LOG_HOST_DIR:-../../data}:/app/access-logs:ro

      # Bind-mount at the same path as on the host
      - /etc/sing-box:/etc/sing-box
      - /etc/wireguard:/etc/wireguard
      - /run/dbus:/run/dbus
      - /run/systemd:/run/systemd
      - /var/log/journal:/var/log/journal:ro
      - /run/log/journal:/run/log/journal:ro

    environment:
      - OGS_LISTEN_ADDR=:${OGS_SLOT_BLUE_PORT:-18080}
      - OGS_DB_PATH=/app/data/stats.db
      - OGS_ACCESS_LOG_PATH=${OGS_ACCESS_LOG_PATH:-/app/access-logs/access.log}

      # Activates DockerLocalExecutor (host D-Bus mode)
      - OGS_EXECUTION_MODE=docker_local
      - OGS_DOCKER_LOCAL_HOST_NETWORK=true
      - OGS_SINGBOX_BINARY_PATH=${OGS_SINGBOX_BINARY_PATH:-}

      - OGS_API_KEY=${OGS_API_KEY}
      - OGS_ADMIN_USER=${OGS_ADMIN_USER}
      - OGS_ADMIN_PASSWORD=${OGS_ADMIN_PASSWORD}
