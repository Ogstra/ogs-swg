services:
  ogs-swg-demo:
    build: .
    image: ogs-swg:demo
    container_name: ogs-swg-demo
    restart: unless-stopped
    cpus: 0.20
    cpu_shares: 128
    mem_limit: 128m
    memswap_limit: 128m
    pids_limit: 64
    security_opt:
      - no-new-privileges:true
    ports:
      - "${OGS_DEMO_PORT:-8119}:8080"
    environment:
      - TZ=${TZ:-America/Argentina/Buenos_Aires}
      - OGS_API_KEY=${OGS_API_KEY:-demo-readonly-key}
      - OGS_API_KEY_READ_ONLY=true
      - OGS_DEMO_DEV_MODE=${OGS_DEMO_DEV_MODE:-false}
      - OGS_DEMO_MODE=true
      - OGS_DISABLE_PASSWORD_LOGIN=true
      - OGS_PUBLIC_IP=203.0.113.10
      - DEMO_LIVE_INTERVAL_SEC=${DEMO_LIVE_INTERVAL_SEC:-60}
      - DEMO_LOG_INTERVAL_SEC=${DEMO_LOG_INTERVAL_SEC:-3}
      - DEMO_SUB_REQUEST_INTERVAL_SEC=${DEMO_SUB_REQUEST_INTERVAL_SEC:-8}
      - DEMO_RETENTION_SECONDS=${DEMO_RETENTION_SECONDS:-3600}
      - DEMO_MAX_DB_BYTES=${DEMO_MAX_DB_BYTES:-52428800}
    volumes:
      - ./docker/demo:/demo:ro
      - ./data-demo:/app/data
      - ${OGS_EXTERNAL_PROFILE_IP_HOST_DIR:-./homelab/ip}:/app/external-profile-ip:ro
    tmpfs:
      - /tmp:size=16m,noexec,nosuid,nodev
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    command: ["/bin/bash", "/demo/start-demo.sh"]
